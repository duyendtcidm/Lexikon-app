const REGISTER = { path: '/', name: 'register.title' }
const LOG_IN = { path: '/log_in', name: 'log_in.title' }
const HOME = { path: '/home', name: 'home.title' }
const LOOK_UP = { path: '/look_up/', name: 'look_up.title' }
const PRACTICE = { path: '/practice/', name: 'practice.title' }
const RANKING = { path: '/ranking/', name: 'ranking.title' }
const STATISTICS = { path: '/statistics/', name: 'statistics.title' }

export {
    REGISTER,
    LOG_IN,
    HOME,
    LOOK_UP,
    PRACTICE,
    RANKING,
    STATISTICS
}