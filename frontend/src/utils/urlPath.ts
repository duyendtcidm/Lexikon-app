const REGISTER = { path: '/register', name: 'register.title' }
const LOG_IN = { path: '/log_in', name: 'log_in.title' }
const FORGOT_PASSWORD = { path: '/forgot_password', name: 'forgot_password.title' }
const HOME = { path: '/', name: 'home.title' }
const LOOK_UP = { path: '/look_up/', name: 'look_up.title' }
const PRACTICE = { path: '/practice/', name: 'practice.title' }
const RANKING = { path: '/ranking/', name: 'ranking.title' }
const EXAM = { path: '/exam/', name: 'exam.title' }

const DISABLE_NAVIGATION = [
    HOME.path
]

export {
    REGISTER,
    LOG_IN,
    FORGOT_PASSWORD,
    HOME,
    LOOK_UP,
    PRACTICE,
    RANKING,
    EXAM
}